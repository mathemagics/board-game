import styled from "styled-components";

const CARD_SIZE = 40;

export const Container = styled.button`
  border: 1px solid black;
  border-radius: 2px;
  height: ${CARD_SIZE}px;
  width: ${CARD_SIZE}px;
`;
